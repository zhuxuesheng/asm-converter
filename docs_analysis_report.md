# 汇编调用约定文档分析报告

**日期**: 2026年2月5日
**分析对象**: `asm-converter` 仓库技术文档

通过对仓库中文档的详细分析，发现以下几处技术错误和容易引起混淆的描述。这些问题主要集中在 **x64 Microsoft 调用约定** 和 **ARM64 AAPCS64 调用约定** 的代码示例中，建议优先修正。

## 1. 严重错误：Microsoft x64 栈对齐计算错误

*   **文件位置**: `docs/assembly-calling-conventions/02-x86-x64/x64-microsoft.md`
*   **章节**: `参数传递示例` -> `Intel/NASM 语法` -> `caller_example`

### 问题描述
在 `caller_example` 中，代码试图分配栈空间并对齐，但计算结果会导致栈**未对齐**，这违反了 Microsoft x64 ABI 的要求（CALL 指令前 RSP 必须 16 字节对齐）。

```nasm
    ; 分配Shadow Space + 栈参数空间
    ; Shadow Space: 32字节
    ; 栈参数: 2个 × 8字节 = 16字节
    ; 总计: 48字节，但需要16字节对齐
    sub     rsp, 48 + 8         ; 56字节（对齐到16字节） <--- 错误！
```

### 详细分析
1.  在 `push rbp` 和 `mov rbp, rsp` 之后，`RSP` 已经是 **16 字节对齐**的（CALL 压入 8 字节返回地址 + PUSH RBP 压入 8 字节 = 16 字节）。
2.  需要分配的空间是 32 (Shadow Space) + 16 (栈参数) = 48 字节。
3.  `48` 正好是 16 的倍数 (`16 * 3 = 48`)。
4.  代码中额外增加的 8 字节导致总分配量为 `56`。
5.  `56` **不是** 16 的倍数 (`56 = 16 * 3 + 8`)。
6.  执行 `sub rsp, 56` 后，RSP 将变为 8 字节对齐，导致后续调用可能崩溃（特别是如果被调用函数使用了 SSE/AVX 指令）。

### 修正建议
直接减去 48 即可保持对齐。

```nasm
    sub     rsp, 48             ; 48字节（已对齐到16字节）
```

---

## 2. 严重错误：ARM64 栈参数传递位置错误

*   **文件位置**: `docs/assembly-calling-conventions/03-arm/arm64-aapcs64.md`
*   **章节**: `参数传递示例` -> `调用示例` (`caller_example`)

### 问题描述
示例代码中，栈参数被放置在了错误的位置。被调用函数 (`sum_ten`) 将会读取到错误的参数值（实际上会读取到调用者保存的 FP 和 LR）。

```asm
caller_example:
    STP     X29, X30, [SP, #-32]!   // 保存FP和LR，分配栈空间
    MOV     X29, SP                  // 建立帧指针
    
    // 设置栈参数（第9和第10个参数）
    MOV     X9, #9
    STR     X9, [SP, #16]           // 参数 i = 9   <--- 错误位置
    MOV     X9, #10
    STR     X9, [SP, #24]           // 参数 j = 10  <--- 错误位置
    
    // ...
    BL      sum_ten
```

### 详细分析
1.  `caller_example` 使用 `STP ... [SP, #-32]!` 分配了 32 字节栈空间，并将 FP/LR 保存到了 `[SP, #0]` 和 `[SP, #8]`（即当前栈底）。
2.  示例代码将参数 `i` 和 `j` 存储到了 `[SP, #16]` 和 `[SP, #24]`。
3.  当执行 `BL sum_ten` 时，`SP` 指向 `caller_example` 的栈底。
4.  **AAPCS64 标准规定**：栈参数应位于传入 SP 的上方，即 `[SP, #0]`, `[SP, #8]` 等。
5.  因此，`sum_ten` 读取 `[SP, #0]` 时，实际读取的是 `caller_example` 保存的 `FP`，而不是参数 `i`。

### 修正建议
需要调整栈布局，将 FP/LR 保存在栈的高地址部分，将参数放在低地址（SP 指向的位置）。

```asm
caller_example:
    // 分配 32 字节：16字节用于保存FP/LR，16字节用于传递参数
    // 注意：我们需要将FP/LR保存在高地址，参数放在低地址（SP处）
    
    SUB     SP, SP, #32             // 分配空间
    STP     X29, X30, [SP, #16]     // 保存FP/LR到高地址 [SP+16]
    ADD     X29, SP, #16            // 建立帧指针
    
    // 设置栈参数（第9和第10个参数）放在SP指向的位置
    MOV     X9, #9
    STR     X9, [SP, #0]            // 参数 i = 9 (位于SP)
    MOV     X9, #10
    STR     X9, [SP, #8]            // 参数 j = 10 (位于SP+8)
    
    // ... 设置寄存器参数 ...
    
    BL      sum_ten
    
    // ...
    
    LDP     X29, X30, [SP, #16]     // 恢复FP/LR
    ADD     SP, SP, #32             // 释放空间
    RET
```

---

## 3. 混淆信息：Go 汇编示例与注释矛盾

*   **文件位置**: `docs/assembly-calling-conventions/02-x86-x64/x64-sysv.md`
*   **章节**: `整数参数传递` -> `Go Plan9 语法`

### 问题描述
注释声称使用 Go 1.17+ 寄存器调用约定，但代码实际展示的是基于栈的旧约定（ABI0）。

```asm
// func SumEight(a, b, c, d, e, f, g, h int64) int64
// Go 1.17+ 使用寄存器调用约定，与System V类似但有差异  <--- 注释提及寄存器
TEXT ·SumEight(SB), NOSPLIT, $0-72
    MOVQ    a+0(FP), AX         // AX = a           <--- 代码使用栈(FP)
```

### 详细分析
虽然 Go 1.17+ 内部引入了基于寄存器的 ABIInternal，但手写的汇编代码默认仍然使用基于栈的 ABI0。Go 编译器会自动生成适配器代码，负责在寄存器和栈之间进行转换。如果不指定 `ABIInternal` 标志并直接操作寄存器，代码实际上仍是在操作栈。

### 修正建议
修改注释以消除歧义，明确说明这是 ABI0 兼容写法。

**建议修改为：**
```asm
// Go 1.17+ 内部使用寄存器调用约定，但手写汇编通常仍使用基于栈的 ABI0
// 编译器会自动生成 ABIInternal 到 ABI0 的转换代码
TEXT ·SumEight(SB), NOSPLIT, $0-72
    MOVQ    a+0(FP), AX
```

---

## 4. 描述不准确：ARM64 间接结果寄存器 X8

*   **文件位置**: `docs/assembly-calling-conventions/03-arm/arm64-aapcs64.md`
*   **章节**: `返回值规则` -> `返回值代码示例`

### 问题描述
在 `get_big_struct` 示例中，注释写道 `X8 保持不变`。

```asm
    // X8 保持不变
    RET
```

### 详细分析
`X8` 在 AAPCS64 中是 **Caller-saved**（易失性）寄存器。虽然被调用函数可能碰巧没有修改它，但标准并不要求被调用函数在返回时保留 `X8` 的值。调用者不能假设调用返回后 `X8` 仍然指向原来的结构体地址。

### 修正建议
删除该注释，或改为 `// X8 是调用者保存寄存器，仅作为输入使用`，以避免误导开发者。
